spring:
  cloud:
    nacos:
      config:
        server-addr: 192.168.170.133:8848   # nacos 配置中心所在地址
        file-extension: yaml              # 指定动态配置的文件类型（扩展名）
        prefix: service-teacher

#dataId: service-teacher-dev.yaml
